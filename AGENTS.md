# 项目协作约定

## 跨 session 读取与更新

- 先读 `.codex/state.md`，再读 `docs/ARCHITECTURE.md`、`docs/DESIGN_DECISIONS.md`、`docs/EXPERIMENT_STATUS.md`、`docs/TODO.md`。执行交接模板在 `docs/EXECUTION_HANDOFF.md`。
- 当前原 session 用于讨论，用户自行另建执行 session；本次未自动创建新任务。代码只由一个执行者在同一工作树写入。
- 阶段结束更新状态、TODO与实验索引；目标、已实现行为、证据结论分别记录。常规实现选择无需逐步确认。

## 项目约束

- 用户要求使用 Git 管理本项目。仓库根目录为 `rsi-agent-lab`。
- 修改前检查 Git 状态，保留用户已有修改；按可审查的功能单元创建本地提交。
- 后端使用 Python/FastAPI，前端使用 React/TypeScript。TS 后端基线保存在 Git 提交 `e63b138`。
- 提交前检查 diff，并执行与改动相关的验证。主要命令：`npm test`（pytest）、`npm run build`，真实平台验收使用 `npm run verify:platforms`。
- 不提交真实 `.env`、密钥、`node_modules`、`dist` 或 `artifacts` 中的运行记录；保留 `.env.example`。
- 业务平台和真实模型的接入状态必须如实记录；离线固定流程不能作为真实 LLM 或 RSI 实验结果。
- 后续端到端 pipeline、演示与性能实验默认使用已部署的 ERPNext/Zammad API，不再默认运行已删除的合成 JSON 沙箱实验。用户已要求删除旧业务沙箱，不能恢复旧数据、固定流程或旧沙箱接口；小型注入测试仅验证协议，不构成业务环境。
- 必须区分“真实平台 API”“本项目初始化的演示记录”“有来源依据的真实企业数据”。当前已部署平台中的 RSI 种子记录由本项目生成；未经来源核验，不得称为真实企业数据，也不得假定开源仓库提供生产数据集。
- 真实平台不可达或能力不足时如实说明并修复连接，不得静默回退沙箱、重置平台或重新生成数据来伪装真实 pipeline。外部连接目前只读，不能将平台只读报告当作完整业务写入任务验收。
- 用户另要求三个场景各至少 100 个任务，已建立公开真实历史数据任务库（Olist 财务、CFPB 投诉、Zammad GitHub Issues 技术工单）。它与已部署实例及合成沙箱不同，必须标注来源；只在训练集学习，验证集选型，测试集不得用于学习或反思，gold.json 参考答案不得作为模型输入。300 个任务的工具契约验证不等于 300 次 LLM 成功率测试。
- 模型 HTTP 调用统一放在 `backend/model_client.py`，业务模块只依赖注入接口；按用户要求关闭思考模式（`enable_thinking: false`，OpenRouter 同时发送 `reasoning.enabled: false`）。
- 2026-09-14 最新重构规格为 `docs/trajectory-rsi-refactor-handoff-2026-09-14.md`（TODO P0.5）。可复用图从正常实际执行轨迹诱导，编译时提炼匹配描述/当前参数槽，具体适用逻辑由train反馈修订；禁止以预置family/template/问题关键词决定路由。通用schema/工具协议允许存在，但不新增人工SOP、隐藏业务默认值或固定角色工作流。临时LLM计划、图结构版本、匹配描述版本和实际后续使用须分别记录，不能仅改叙事当作实现。
- 工具文档保持简短，以用途和主要返回字段支持 intent 检索。任务库的 Plan 与执行角色可分别配置模型；并发任务、模型请求、读取工具各自限流并独立计量。未配置独立规划模型时明确标记同模型，不宣称大小模型协同或高负载吞吐收益。
- 确定性任务校验失败时，不得把“执行结束”当作任务成功，也不得自动晋升为新经验。自定义任务未选择完整校验目标时，只检查状态约束并明确标注范围。

- Graph RSI 在线进化使用任务库正常流量，不为图验证额外发起影子 rollout。运行失败先恢复当前任务；只有有依据的结构修改产生后继版本，未知失败保留待分析。恢复模型/工具成本计入任务总量，本地查图和维护耗时单独记录；不得把零额外 rollout 写成零总体 overhead。版本维护只从训练任务生成 Patch，验证任务只累计证据，测试任务不回写经验。

- 每轮工作以导师的质量、效率、递归进化和流程可靠性目标验收，见 docs/mentor-goals.md。使用冻结成对评测时先固定任务和图版本，Strong ReAct 与 RSI 共用通用执行提示，保留全部失败；图覆盖不足如实计入冷启动。报告文字默认未复核，不把结构化通过冒充完整质量相同。
- 前端默认指标必须来自单一发布清单指定的同一experiment/runtime/任务资产。当前正式证据、候选预检和历史诊断分层；禁止在主导航或总览中拼接V17、V4与P0.5数据。历史工件保留审计，不因界面清理而删除或改写。

- LLM-as-Judge 与确定性评分分开保存。裁判隐藏 Agent 身份与成本，使用相同事实证据并交换 A/B 顺序；只允许一次格式修正，不因胜负重试。裁判成本不计入 Agent 执行成本，同模型裁判不能称为独立模型验证。Plan + ReAct 基线与 RSI 共用通用执行提示；Plan 复用和图执行收益必须区分。
