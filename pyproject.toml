[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "evograph-agent-core"
version = "0.1.0"
description = "Minimal runnable Graph RSI mechanism for review"
requires-python = ">=3.9"
dependencies = []
