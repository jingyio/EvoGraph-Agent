# 基础版本验收

当前默认后端已迁移到 Python/FastAPI，49 项 Python 测试与真实平台验收通过，详见 [Python 迁移验证](python-migration.md)。下文保留对应 TS 版本的历史验收结果，不能直接当作 Python 版本性能指标。

## 已执行

- TypeScript 类型检查和 Vite 生产构建。
- 23 个自动测试：金额守恒、部分回款、多发票分配、重复流水阻断、原子回滚、幂等、财务证据、坐席技能/容量/版本、草稿来源、运行隔离、非法工具参数、模型观察回传、运行限额、取消、token 缺失、响应截断、HTTP function-calling 协议、外部数据隔离和平台客户端协议。
- 财务离线完整执行：19 次真实沙箱工具调用，0 个工具错误；应收 ¥66,500，匹配 ¥42,000，剩余 ¥24,500，5 个异常事项。
- 客服离线完整执行：21 次真实沙箱工具调用，0 个工具错误；5 个未关闭工单均已分派、5 个未发送草稿、2 个超时升级，未新增关闭工单。

以上工具调用次数属于固定测试程序，不是 LLM 决策的实际调用数。测试用的协议服务器不是外部模型或真实业务平台。

## 浏览器验收范围

已在默认浏览器视口（1280 × 720）验证财务与客服场景切换、运行按钮、业务结果、报告、运行历史恢复、工具目录、平台配置状态、未配置模型时的禁用行为。生产页面未观察到浏览器 warning/error。CSS 包含窄屏布局，但未进行手机视口专项验收。

已对运行中的生产 HTTP 服务检查 Markdown 导出、离线自定义任务拒绝、缺少模型凭据时拒绝真实执行、外部 Origin 拒绝，以及未配置平台时的连接状态。

## 首次真实模型验证（2026-09-09）

已使用用户配置的 `qwen/qwen3.8-flash` 执行两个预设沙箱任务，并读取落盘运行记录核查结果。财务：12 次模型请求、25 次工具调用（含 3 次校验失败后修正）；客服：8 次模型请求、22 次工具调用，无工具错误。业务状态和用量一致性校验通过；简报存在个别文字统计不准确，不能视为完整质量验收。详见 [验证记录](live-model-validation-2026-09-09.md)。

## 真实平台部署与联调（2026-09-09）

已部署 ERPNext v16.34.2、Zammad 7.1.3-0011。`npm run verify:platforms` 通过，覆盖真实种子记录、多页读取、部分回款分配关系、工单文章/SLA、默认组隔离及两个平台的写入 403。两次真实模型只读任务正常结束且无工具错误，但报告包含平台语义和归因错误，详见 [部署验证](platform-deployment-validation-2026-09-09.md)。初始数据库和必要文件已备份，恢复未演练。

## 尚未覆盖

多样本真实 LLM 质量/延迟评估、多币种与更复杂平台权限、外部平台写入、快照恢复演练、规模化数据集、知识库向量检索、持久记忆、RSI 学习与跨任务泛化。进程中断后的运行恢复、生产身份认证、多用户隔离亦不属于当前本地版本。

## Graph RSI v0.2 更新

2026-09-09 已实现受限读取图学习、影子验证、版本持久化、参数绑定、图执行与 ReAct 回退；加入经验候选检索/模型节点选择，以及 OpenAPI 子集生成工具。7 个生成工具完成真实平台验收，四份真实历史轨迹成功编译验证。Flash 阶段 A/B 受限流阻断；后按用户新配置 `qwen/qwen3.5-27b` 关闭思考完成单次同快照配对：ReAct 16 次、Graph 12 次，token 88,105 与 61,990，财务状态一致。另一次客服任务改写验证了经验选择和新图保存，但漏掉两个超时升级，完整质量不通过。模型 HTTP 已集中到 model-client.ts，实际响应 reasoning token 为 0，测试扩展至 40 项并通过。详情见 [Graph RSI 验证记录](graph-rsi-validation-2026-09-09.md)。当前仍只完成明确限定的读取图原型，未证明多任务平均收益。
