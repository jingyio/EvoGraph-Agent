# V1负向诊断与V2共享协议修复

V1预检 `0795dece-988a-42f0-910a-45480aa50a3b` 在首对财务任务后质量停止。
Baseline两次发布均因分组证据引用缺少workspace前缀被拒绝，其金额、计数、订单ID
与私有核验一致；结果仍为失败，不能事后改成成功。RSI同题通过，消耗更多token。

| Agent | 通过 | token | 模型请求 | 工具调用 | 工具错误/拒绝 | 耗时ms |
|---|---:|---:|---:|---:|---:|---:|
| Baseline | 0/1 | 56,841 | 5 | 8 | 0 | 125,291 |
| RSI | 1/1 | 162,600 | 12 | 17 | 2 | 89,190.051 |

usage完整，无provider重试。全尝试token差为RSI **+186.06%**，且质量门槛失败；
无效率结论。RSI从成功收据创建G0/M0，未有后续使用或结构/匹配进化。没有继续客服、
技术工单或扩大对照。运行期间展示曾把未终态pair画入曲线，V2读取层已纠正，旧运行不改写。

## V2修复依据与冻结安排

实际根因是旧共享上下文在资料读齐后删除冗长 `_evidenceRef`，但仍保留 `rowId`；
旧发布规范化仅处理顶层evidenceIds，新groups没有对应处理。V2只将组内缩写在本次
observed证据中唯一匹配时补齐前缀，记录原始/规范化映射；未知或歧义引用仍拒绝。
不查gold，不改变金额/清单/计数，不把修复当作RSI进化。

附带协议修复：当前公开资料范围单独保存，P0.5运行时的证据进度/规范化不读取私有
requiredEvidenceIds；显式空rightTerms与空rightAliases同样表示没有右侧；编译器允许
已执行代数单位系数±1，不把其误认作缺少业务来源的阈值；结构比较忽略独立节点
排列和槽编号，防止重排虚增G版本。按键证据保留不同关联键名且去重。

V2为新的隔离空经验运行，重新冻结源码指纹，继续同一批公开业务资产的预检；这是
明确协议修复后的新版本，不在旧运行中重试或选最好结果。所有V1失败与开销单列。
其余预算、先财务后推广顺序和停止门槛沿用V1协议。未实现的完整output依赖编译、
本地产物图执行、独立M-only修订及第二代表工作流组仍不宣称完成。
