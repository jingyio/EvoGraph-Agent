# 财务与技术工单 Graph RSI 归因协议（2026-09-15）

## 目标

新的候选发布只覆盖财务运营和技术工单运营。客服运行及失败完整保留在旧三场景实验 `4a0b156d-a016-4f61-ab22-909d9ec0ca06` 的历史审计中，不进入本协议的任务、指标或发布结论。本协议不是从旧实验删除失败点，而是在新实验运行前冻结两个明确场景。

## 固定任务

资产继续使用 `cross-domain-rsi-attribution-v1-48` 的原题面、附件和私有评分，不改写旧文件。

- 预检：`F01, F02, T01, T02`，分别覆盖两个场景的冷启动与首次复用。
- 正式：`F01–F12` 加 `T01–T12`，共24个不同 train 任务；财务12项、技术工单12项。
- 客服任务不在新清单内；技术工单的活动分诊与交接准备度两个子簇均进入正式清单。

清单在实验创建时写入 `experiment.json` 和冻结资产副本；运行中不能换题、重排或按结果删点。

## 对照和执行

- A：图执行 · 不学习。每项重新规划和编译，经验库保持空。
- B：图执行 · 在线 RSI。从独立空经验开始，只学习本实验此前成功 train 任务。
- 两臂共享模型 `qwen/qwen3.5-27b`、thinking关闭、题面、附件、工具、提示、预算、恢复与评分。
- 主 Key 运行 A，Secondary Key 运行 B；每一对两臂并行发起。
- 全局并发上限为 `run=2, model=2, read=1`；单臂仍为 `1/1/1`。保存的单臂串行时长用于比较，双Key只缩短实验墙钟时间。

## 门槛

正式实验必须由同一runtime和资产的四对预检解锁。预检要求两臂全部通过、usage完整、经验维护正常，在线臂真实图执行率至少50%。

正式24对保留全部失败和开销。严格同质量成本结论要求两臂24/24、usage完整且图执行率达标；否则只报告准确率、绝对token、模型请求、工具和单臂串行时长。版本创建、修订与后续实际使用分别报告，生成版本不自动等于效率进化。
