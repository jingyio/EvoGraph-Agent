# 候选证据

## 发布身份

| 字段 | 值 |
|---|---|
| 状态 | `candidate` |
| 实验 | `d02f0ecd-8bb5-4359-94be-e7f7233df6a5` |
| 任务资产 | `finance-rsi-attribution-v5-12` |
| runtime | `sha256:11e6f73013a0ad50c4023b637214660087c001cff55a676c354e7aba299d68f4` |
| 模型 | `qwen/qwen3.5-27b`，关闭 thinking |
| 协议 | 同一图执行 Agent；一臂禁止跨任务读写，另一臂从独立空经验在线学习；严格串行 `run=model=read=1` |

## 保存结果

12 个固定财务任务中，图执行但不学习的 Agent 为 11/12 通过，在线 RSI 为 12/12 通过。两臂 usage 完整，失败与恢复开销均已保留。

| 指标 | 不学习 | 在线 RSI |
|---|---:|---:|
| Agent token | 2,119,742 | 936,718 |
| 模型请求 | 165 | 58 |
| 工具调用 | 250 | 216 |
| 工具错误 | 46 | 8 |
| 串行时长 | 1,081.771 秒 | 742.943 秒 |

由于通过率不同，这些是同一冻结任务组的实际观察，不是严格同质量的效率结论。

运行前冻结的订单复核子簇 FX01–FX08 中，两臂均 8/8 通过：token 从 1,025,754 降至 366,230，模型请求从 84 降至 30，串行时长从 649.760 秒降至 383.985 秒。该子簇支持同质量条件下的局部效率观察，但仍限于当前候选范围。

## 递归进化链

- FX01：从空经验产生 13 节点 `G0/M0`。
- FX09：实际使用 `G0/M0` 后形成 15 节点 `G1/M1`。
- FX10：实际执行 `G1/M1`。
- FX11：使用 `G1`，形成 23 节点 `G2/M2`。
- FX12：实际执行 `G2/M2` 的全部节点。

这条链证明保存版本经历了创建、正常成功后的覆盖扩展和后续实际使用。它不证明长期稳定、跨场景泛化或零错误。

## 限制

- 当前只覆盖 12 个财务任务；尚未形成跨业务正式发布。
- 没有独立 Judge 或人工全文审核；结构化事实与证据通过不等于报告正文完全审核。
- 候选组两臂质量不同，不可将全量 token 差作为严格消融结论。
- 原始 run、失败记录、版本 diff 和协议保存在 [`develop`](https://github.com/jingyio/EvoGraph-Agent/tree/develop) 分支，未被这个展示分支删除。
