# 真实平台 API Pipeline 重跑

日期：2026-09-10（Asia/Shanghai）。批次 `7e38a73a-c51f-46d1-b549-36b13ae71d6a`。

## 数据来源与默认工作方式

本次直接读取 v100 上已部署的 ERPNext 和 Zammad，不使用 JSON 沙箱业务工具，没有重置、重新播种或修改平台记录。恢复了本地 SSH 转发后，两个平台的只读端点均验证可达。

当前可见记录仍为本项目初始化的 RSI 种子记录：ERPNext 6 张发票、6 笔收款；Zammad 6 个工单、5 个未关闭。此处“真实”指业务软件、数据库、API 与模型调用真实运行，不代表数据来自企业生产环境。现有仓库没有经核验的真实企业数据集。真实企业数据实验仍需有来源的脱敏数据或授权业务实例。

已在 AGENTS.md 写明：后续端到端任务优先使用部署平台，不默认继续 JSON 合成稳定性/进化实验；沙箱保留为明确标注的单元/回归环境，不得静默回退或把种子记录称为真实企业数据。

## 可复现入口

运行 `npm run pipeline:platforms`。入口为 `backend/platform_pipeline.py`：

1. 分页盘点平台当前记录，记录来源判断与列表状态指纹。
2. 固定本批次巡检时间，用已配置模型完成 ReAct 只读任务。
3. 从本次 ReAct 轨迹学习读取图，在实际 API 上进行影子读取验证。
4. 相同任务、相同巡检时间执行 Graph RSI。
5. 再次盘点列表状态，核对前后指纹并落盘。该检查覆盖已读取列表字段，不是全库审计。

模型 HTTP 继续统一走 model_client.py，关闭 thinking，允许批量工具调用。没有调用外部写工具、发送消息或将当前沙箱负 motif 用于不匹配的平台。运行失败保留原始结果，不自动重试或回退沙箱。

原始批次：`artifacts/platform-pipelines/7e38a73a-c51f-46d1-b549-36b13ae71d6a.json`。各任务完整轨迹、模型用量与报告保存在 `artifacts/<run-id>/`。这些文件私有且被 Git 忽略。

## 评估范围

外部平台评分仍标记为 `not_evaluated/external_read_only`。pipeline 执行完成只证明接口调用、图执行、报告保存链路正常；不自动证明完整报告质量、业务写入成功或平均性能收益。学习成本需另列，Graph 在线成本不包含本次采集来源轨迹和影子验证的成本。

## 实际结果

模型 qwen/qwen3.5-27b；每次返回 reasoning token 均为 0。

| 平台 | 策略 | LLM 请求 | 工具调用 | 工具错误 | 输入 / 输出 token | 耗时 |
|---|---|---:|---:|---:|---:|---:|
| ERPNext | ReAct | 5 | 9 | 0 | 28,350 / 2,883 | 54.616 s |
| ERPNext | Graph | 2 | 9 | 0 | 21,139 / 2,392 | 31.210 s |
| Zammad | ReAct | 5 | 14 | 0 | 47,672 / 2,782 | 41.363 s |
| Zammad | Graph | 2 | 16 | 0 | 29,264 / 1,744 | 29.173 s |

对应运行 ID：ERPNext `e4717bfe-adc7-4f76-97ba-44d78ab340db`、`e22d0f58-e084-439a-84d7-73531337a16a`；Zammad `e03a2bfb-383a-4bde-a19e-ae1d27148c07`、`22bb028a-53d6-49ad-92c5-fee104bbd86d`。两平台盘点列表前后指纹均一致，四次均保存了报告。

ERPNext 新图包含 3 个节点、8 次读取；影子验证额外 8 次 API 工具调用，452 ms。Zammad 新图包含 5 个节点、15 次读取；影子验证额外 15 次调用，1,829 ms。两次学习均为 0 次额外 LLM，在线图精确命中、规划器调用 0 次。Zammad 的 foreach 从全量列表遍历，额外读取了一张已关闭工单的详情与文章，因此比仅查询未关闭工单的 ReAct 多 2 次工具调用。

人工事实核对：ERPNext 两份报告的发票总额 66,500 CNY、未结清 24,500、已有分配 42,000 与盘点一致；收款记录总额含重复 reference_no 的待核查记录，不应解释为已核实新增回款。Zammad 两份报告均识别 5 个未关闭、4 个未分派，但 Graph 摘要把总可见工单数量写成 7，实际只有 6（最大 ID 为 7）。这说明 0 工具错误不等于报告无事实错误，未将报告改写来掩盖问题。SLA 违约结论也仍需独立平台语义评分。
