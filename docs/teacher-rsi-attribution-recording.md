# 三分钟录制脚本

入口：`http://127.0.0.1:5173/#analysis`。默认选择质量受限候选；所有运行过程都是保存轨迹回放，不制作假实时动画。

**0:00–0:25｜任务与对照。** 展示数字员工中的自然财务请求和Olist公开历史附件。说明A“图执行·不学习”和B“图执行·在线RSI”使用同一27B模型、提示、工具、恢复、预算和输入；唯一差异是跨任务经验。

**0:25–0:50｜先讲质量边界。** 进入数据分析，指向release、实验ID、runtime和candidate状态。两任务预检通过后完成12对正式运行；在线RSI 12/12，不学习11/12。FX11不学习臂达到24次请求上限，因此质量门槛失败，页面不把累计差包装成formal收益。

**0:50–1:15｜创建与稳定复用。** 点击FX01：两臂均通过，在线臂从空库创建13节点G0/M0，冷启动token基本持平。沿时间线展示FX02–FX08实际执行G0；在线RSI每题3次模型请求，所有业务值从当前附件重新计算。

**1:15–1:45｜第一次覆盖扩展。** 点击FX09，展开G0→G1 diff：支付 `order_id` 频次、订单状态和月份统计进入15节点G1/M1。运行中的错误记录计数聚合被语义守卫拒绝，没有进入版本。点击FX10确认G1/M1后续实际执行，4次请求通过。

**1:45–2:15｜失败、恢复与第二次进化。** 点击FX11并对照两臂。A反复选择错误计数路径，24请求后失败；B使用G1，经过有界补全以17请求通过并生成23节点G2/M2。明确G2仍含重复筛选，是可用但非最小的恢复模板。

**2:15–2:35｜修订后验证。** 点击FX12：实际执行G2/M2全部23节点，以当前阈值重绑定，2次模型请求、24次工具调用、0工具错误通过。只有这一步完成后，才主张FX11修订获得后续使用。

**2:35–2:55｜成本与请求位置。** 展示绝对总量：token 2,119,742→936,718，请求165→58，工具错误46→8，串行时长1081.771→742.943秒。计算选择与参数绑定请求132→34，报告组合11→12；收益来自重复决策减少，报告没有被规则替代。

**2:55–3:00｜结论限制。** 本轮证明财务子簇的大规模复用、两条修订后使用链和RSI可靠性差异。由于A只有11/12，不能宣称同质量正式净收益；尚无客服/技术工单、独立Judge、人工全文复核或长期稳定性结论。
